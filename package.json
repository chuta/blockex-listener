{
  "name": "blockex-listener",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "SendGrid Inbound Parse proxy that injects x-webhook-key and forwards to Netlify listeners.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  }
}

